# Plan 003: Clear all pnpm-audit advisories by bumping the dev-only toolchain

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**: `git diff --stat 725149a..HEAD -- package.json pnpm-lock.yaml vitest.config.ts tests/`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P2
- **Effort**: M
- **Risk**: MED — vitest 2→3 and happy-dom 15→20 are major bumps under 19 spec files
- **Depends on**: none (but MUST land before plan 004, which writes new tests on this stack)
- **Category**: dependencies
- **Planned at**: commit `725149a`, 2026-06-10

## Why this matters

`pnpm audit` reports 9 advisories — 3 critical, 2 high, 3 moderate, 1 low — **all in
devDependencies** (production deps `@xterm/*` and `fuse.js` are clean). The criticals
are in `vitest` 2.x and `happy-dom` <20, both of which execute with full user
privileges on the dev machine on every `pnpm test`. There is no end-user exposure,
but the dev-loop exposure is real and the fix is mechanical. After this plan,
`pnpm audit` reports zero known vulnerabilities and the full test suite passes on
the upgraded stack.

## Current state

Audit summary at planning time (`pnpm audit`):

| Severity | Package            | Vulnerable      | Fixed in   | Path                       |
|----------|--------------------|-----------------|------------|----------------------------|
| critical | vitest             | >=2.0.0 <2.1.9  | >=2.1.9    | `.>vitest`                 |
| critical | vitest             | <3.2.6          | >=3.2.6    | `.>vitest`                 |
| critical | happy-dom          | <20.0.0         | >=20.0.0   | `.>happy-dom`              |
| high     | happy-dom          | <20.8.9         | >=20.8.9   | `.>happy-dom`              |
| high     | happy-dom          | >=15.10.0 <=20.8.7 | >=20.8.8 | `.>happy-dom`             |
| moderate | vite (transitive)  | <=6.4.1         | >=6.4.2    | `.>vitest>vite`            |
| moderate | esbuild (transitive)| <=0.24.2       | >=0.25.0   | `.>vitest>vite>esbuild`    |
| moderate | js-yaml            | >=4.0.0 <4.1.1  | >=4.1.1    | `.>js-yaml`                |
| low      | @eslint/plugin-kit | <0.3.4          | >=0.3.4    | `.>eslint>@eslint/plugin-kit` |

Relevant pinned devDependencies in `package.json` (the repo pins exact versions —
no `^`/`~`; keep that convention):

- `happy-dom: 15.11.0`, `js-yaml: 4.1.0`, `eslint: 9.15.0`, `esbuild: 0.25.0` (root
  esbuild is already fine; the flagged one is vite's transitive copy).
- vitest's exact pin is in `package.json` (2.x line per the advisory match).

Test infrastructure that the bumps touch:

- `vitest.config.ts` — custom inline plugin (`yamlAsText` transform), happy-dom
  environment, `tests/setup.ts` setupFile, an `obsidian` → `tests/stubs/obsidian.ts`
  resolve alias. All standard vitest config surface; vitest 3 keeps these APIs.
- 19 spec files in `tests/*.spec.ts`, 150 tests; DOM-heavy specs
  (`tests/a11y-audit.spec.ts` runs axe-core against happy-dom; component specs build
  real DOM) are the most likely to notice happy-dom 15→20 behavior changes.
- Test scripts: `pnpm test` runs `vitest run`.

## Commands you will need

| Purpose   | Command                | Expected on success |
|-----------|------------------------|---------------------|
| Install   | `pnpm install`         | exit 0              |
| Audit     | `pnpm audit`           | "No known vulnerabilities found" (end state) |
| Typecheck | `pnpm typecheck`       | exit 0              |
| Tests     | `pnpm test`            | 150 tests pass (19 files) |
| Lint      | `pnpm lint`            | exit 0              |
| Build     | `pnpm build`           | exit 0              |
| Verify    | `pnpm verify`          | exit 0              |

## Scope

**In scope** (the only files you should modify):
- `package.json` (devDependencies versions only)
- `pnpm-lock.yaml` (regenerated by pnpm)
- `vitest.config.ts` (only if vitest 3 requires a config adjustment)
- `tests/setup.ts` and individual `tests/*.spec.ts` (only mechanical fixes required
  by the new happy-dom/vitest behavior — see STOP conditions for the limit)

**Out of scope** (do NOT touch):
- `dependencies` (production): `@xterm/addon-fit`, `@xterm/xterm`, `fuse.js`.
- Root `esbuild` pin (0.25.0 — already clean; the build pipeline depends on it).
- `src/**` — a dev-toolchain bump must not require source changes.
- TypeScript version — `typescript: 5.4.5` stays unless vitest 3 hard-requires newer
  (it doesn't as of 3.2.x).

## Git workflow

- Conventional commits to `main`. One commit:
  `chore: bump vitest 3.x + happy-dom 20.x + js-yaml/eslint to clear audit advisories`.
- Do NOT push unless the operator instructed it.

## Steps

### Step 1: Record the baseline

```bash
pnpm test 2>&1 | tail -5
```

**Verify**: 150 passing tests across 19 files. Record the exact counts — the end
state must match.

### Step 2: Bump the four packages

In `package.json` devDependencies, set (keeping the repo's exact-pin style):

- `vitest` → latest 3.x (must be `>=3.2.6`); check with `pnpm view vitest version`
- `happy-dom` → latest 20.x (must be `>=20.8.9`); check with `pnpm view happy-dom version`
- `js-yaml` → `4.1.1`
- `eslint` → latest 9.x (pulls `@eslint/plugin-kit >=0.3.4`); check `pnpm view eslint version`

Then:

```bash
pnpm install
```

**Verify**: exit 0, lockfile regenerated.

### Step 3: Audit gate

```bash
pnpm audit
```

**Verify**: zero advisories. If any remain, identify the path; transitive leftovers
under vitest/vite usually clear with `pnpm update --latest vitest` — if one persists
and is NOT fixable inside the in-scope files, record it in the plan's status note
rather than forcing it.

### Step 4: Full suite on the new stack

```bash
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm verify
```

**Verify**: all exit 0; test count matches Step 1 (150/19).

If `pnpm test` fails: classify each failure —
- **Mechanical** (changed default, renamed option, stricter DOM behavior — e.g.
  happy-dom now implementing a previously-stubbed API): fix in the spec or
  `tests/setup.ts`/`vitest.config.ts`, keeping assertions' intent identical.
- **Behavioral** (a test now exercises genuinely different logic, or an a11y-audit
  result changes): STOP condition.

### Step 5: eslint sanity on the bumped eslint

`eslint.config.mjs` uses flat config with `eslint-plugin-obsidianmd@0.2.8`.

```bash
pnpm lint
```

**Verify**: exit 0 with no NEW rule failures. If the eslint minor bump surfaces new
findings from existing rules, STOP and report (do not "fix" src/ — out of scope).

## Test plan

No new tests. The deliverable is the existing 150 tests green on the patched stack
plus a clean `pnpm audit`.

## Done criteria

- [ ] `pnpm audit` → no known vulnerabilities (or documented unfixable transitive, see Step 3)
- [ ] `pnpm test` → same pass count as baseline (150 tests / 19 files)
- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm build`, `pnpm verify` all exit 0
- [ ] `git diff package.json` shows ONLY the four devDependency lines changed
- [ ] No `src/**` files modified (`git status`)
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:

- More than ~5 spec files fail after the bump, or any failure is behavioral rather
  than mechanical (Step 4 classification).
- The fix would require modifying `src/**` or production dependencies.
- `tests/a11y-audit.spec.ts` reports different axe violations on happy-dom 20 —
  that's a real signal about the components, not a test-stack issue; it needs a
  human decision.
- vitest 3 cannot load the custom `yamlAsText` plugin or the `obsidian` alias in
  `vitest.config.ts` after a reasonable fix attempt.

## Maintenance notes

- Plan 004 (updater tests) builds on this stack — land this first.
- Reviewer should scrutinize: that test assertions weren't weakened to make the
  suite pass (intent-identical fixes only).
- Deferred: moving version pins to ranges or adding Renovate/Dependabot — the repo
  deliberately exact-pins; changing that policy is an operator decision, not a chore.
